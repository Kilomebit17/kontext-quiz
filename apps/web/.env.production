# Public URL of the deployed site: used for canonical/Open Graph tags, robots.txt and sitemap.xml.
# Override at build time (e.g. Docker build-arg) when the domain changes.
VITE_SITE_URL=https://kontext-quiz.mooo.com
